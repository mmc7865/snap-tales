<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="../src/style.css" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "suse";
      }
      html,
      body {
        width: 100%;
        height: 100%;
      }
      #main{
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #ecf0f3;        
      }
      form{
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 10px;
        padding: 20px;
        /* border: 2px solid red; */
        border-radius: 10px;
        box-shadow: -3px -3px 7px #fff, 3px 3px 5px #ceced1;
    }
    input{
        padding: 10px 20px;
        border-radius: 20px;
        border: none;
        outline: none;
        font-size: 16px;
        font-weight: 600;
        background-color: #ecf0f3;
        box-shadow: inset 3px 3px 5px #ceced1, 3px 3px 7px #fff;
    }
    button{
        padding: 10px 30px;
        margin-top: 10px;
        width: 150px;
        border-radius: 20px;
        border: none;
        outline: none;
        font-size: 18px;
        font-weight: 600;
        background-color: #ecf0f3;
        box-shadow: -3px -3px 7px #fff, 3px 3px 5px #ceced1;
        cursor: pointer;
        color: #00000090;
        transition: all .3s;
    }
    button:active{
          box-shadow: inset 3px 3px 5px #ceced1, 3px 3px 7px #fff;
          color: #000;
      }
      span{
        margin-top: 10px;

      }
      a{
        text-decoration: none;
        font-weight: 500;
      }
      a:hover{
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div id="main">
      <form action="/users/login" method="post">
        <h1>Sign In</h1>
        <input required name="email" type="text" placeholder="Email" />
        <input required name="password" type="password" placeholder="Password" />
        <button>Sign In</button>
        <span>Create a new account? <a href="/users/register">Sign Up</a></span>
        
      </form>
    </div>
  </body>
</html>
